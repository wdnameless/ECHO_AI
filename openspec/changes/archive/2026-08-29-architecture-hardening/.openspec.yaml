schema: spec-driven
created: 2026-08-29
archived: 2026-08-29
title: "Architecture Hardening — Round 2"
status: partial
notes: >
  Изменение перенесено в archive, но при сверке с кодом выполнено частично.
  Состояние по пунктам (сверено 2026-09-16):

  ВЫПОЛНЕНО: 1 (Job Object), 3 (микрофон через WS), 5 (порт-дискавери),
  7 (error surface), 2.2-2.3 (таймер эмиссии перенесён в Worker),
  4.1-4.3 (код-сплиттинг: стартовый бандл 1.98 -> 0.93 МБ),
  6 (Self-Evolution в SQLite — схема сведена к `se_style` + `se_feedback_log`).

  ОТКЛОНЕНО СОЗНАТЕЛЬНО:
  - 2.1 (серверный дедлайн эмиссии) — дублировал бы клиентскую логику;
    дефект устранён на клиенте переносом таймера в Worker (2.2).
  - 8 (третья сессия) — помечена «опционально» в исходном плане,
    потребителя у неё нет; две сессии покрывают двусторонний стрим.

  НЕ ВЫПОЛНЕНО: 1.2 закрыта отдельно (проверено 2026-09-16: force-kill
  приложения убивает сайдкар за 4 с).
