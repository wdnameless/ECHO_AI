# Requirements Manifest — perf-latency-wave

## Verbatim user quotes (source of truth)

> «Опиши архитектуру нашу и скажи что можно улучшить для скорости и отзывчивости и и оптимизации»
> «Фикси все»
> «не распознается потом, огромная задержка и тд много проблем»
> «У нас всегда должен быть портабл режим, где все в папке в которую юзер установил\распоковал, запустил аппку»

## Requirements

| ID | Requirement | Verbatim source | Status |
|----|-------------|-----------------|--------|
| R01 | Сокращение VAD-паузы тишины и устойчивый к шуму VAD (адаптивный noise-floor вместо фиксированного RMS-порога): ответ ИИ быстрее на ~500мс после реплики собеседника | «сказь что можно улучшить для скорости и отзывчивости» + «Фикси все» (п.1 предложенного плана) | in-spec |
| R02 | KV-Cache Alignment системного промпта: статичная часть (base, правила, RAG) идёт первой и байт-стабильна между запросами; волатильные блоки (язык, self-evolution, поиск) — в конец | «Фикси все» (п.2) | in-spec |
| R03 | Устранение дорогой сериализации аудиофреймов через IPC: `speech-frame` передаётся base64-строкой вместо JSON-массива чисел (16KB PCM → 60-80KB JSON → 22KB base64); полный нативный WS-клиент Rust→sidecar (новая зависимость tokio-tungstenite + редизайн asr-gate) — deferred | «Фикси все» (п.3) | in-spec (адаптировано) |
| R03i | Нативный стриминг PCM Rust→ASR напрямую без WebView (требует WS-клиента в Rust и редизайна гейта) | «Фикси все» (п.3) | deferred — отдельная волна; текущий JS-WS уже буферизован, выигрыш ~50мс не оправдывает редизайн протокола в этой итерации |
| R04 | Pre-warming HTTP/TLS-соединения к LLM: команда `warm_llm_connection(url)` в Rust, вызывается при старте речи/арме gap-таймера — соединение открыто до финализации вопроса | «Фикси все» (п.4) | in-spec |
| R05 | Потоковый (добounced) перевод парциальных транскрипций: спиннер заменяется живым переводом по мере распознавания, дебаунс 400мс | «Фикси все» (п.5) | in-spec |
| R06 | SQLite `PRAGMA journal_mode = WAL` (+ synchronous NORMAL, temp_store MEMORY, cache_size) применяется через rusqlite на старте для всех путей БД — неблокирующие записи истории | «Фикси все» (п.6) | in-spec |
| R07 | Декомпозиция монолитного `app.context.tsx` (1037 строк) на Zustand-сторы | «Фикси все» (п.7) | deferred — рефакторинг 1037 строк с высоким риском регрессий при маргинальном визуальном выигрыше; требует отдельной волны с полным регрессионным прогоном |
| R08 | Регрессионная безопасность: все существующие тесты (304 vitest + 47 cargo) остаются зелёными, портативный режим и шифрование секретов не ломаются | «У нас всегда должен быть портабл режим» | in-spec |

## Deferred (не вошло в эту волну)

- R03i — нативный WS-клиент Rust→ASR: требует `tokio-tungstenite` и переделки `asr-gate` (JS-гейт перестаёт видеть владельца стрима). Записано как отдельная задача.
- R07 — Zustand-декомпозиция: рефакторинг `app.context.tsx` (1037 строк) с риском регрессий. Триггер апгрейда: заметные просадки FPS при активной речи в замерах.
