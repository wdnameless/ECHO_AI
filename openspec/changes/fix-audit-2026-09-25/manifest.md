# Requirements Manifest — Fix all audit findings (2026-09-25)

Источник: сессия 2026-09-25. Аудит — `.workflow/audit-2026-09-25-architecture.md`
(4 read-only ревьюера + верификация оркестратором на указанных строках).
Каждая строка выведена из дословной цитаты пользователя ниже.

## Запросы пользователя (verbatim)

- «Поищи проблемы и баги архитектурные»
- «Фикси все по порядку»

## Реестр требований

Порядок = порядок из аудита (P0 → P1 → P2), как требует «по порядку».

### P0 — потеря данных и тихая смерть пайплайна

| id | Требование | Цитата-источник | Статус | Слайс |
|---|---|---|---|---|
| R01 | Сохранение чата с >100 сообщениями не удаляет сообщения, которые должны остаться (сейчас: 0 строк после цикла) | «Фикси все по порядку» + аудит P0-1 | done | S1 |
| R02 | Регрессионный тест на 150 сообщений: после сохранения в БД ровно 150 строк | выведено из R01 (неявное, `i`) | done | S1 |
| R03 | 3 секунды тишины в системном звуке не завершают захват навсегда: таймаут — не фатальная ошибка | «Фикси все по порядку» + аудит P0-2 | done | S2 |
| R04 | UI не показывает «идёт запись», когда поток захвата мёртв | выведено из R03 (неявное, `i`) | done | S2 |
| R05 | Зависший, но живой sidecar (pid жив, порт мёртв) перезапускается watchdog'ом по порогу | «Фикси все по порядку» + аудит P0-3 | done | S3 |

### P1 — неверные ответы, утечки, блокировки

| id | Требование | Цитата-источник | Статус | Слайс |
|---|---|---|---|---|
| R06 | История диалога уходит в LLM в хронологическом порядке (сейчас — задом наперёд) | «Фикси все по порядку» + аудит P1-4 | done | S1 |
| R07 | Выход из приложения не блокирует главный поток до 8 секунд | «Фикси все по порядку» + аудит P1-5 | done | S3 |
| R08 | Удаление лицензии сначала освобождает место на сервере, потом стирает ключи | «Фикси все по порядку» + аудит P1-6 | done | S4 |
| R09 | Tauri-слушатели непрерывной записи снимаются при unmount (сейчас — утечка 5 штук) | «Фикси все по порядку» + аудит P1-7 | done | S4 |
| R10 | Сохранение разговора не теряет сообщения, добавленные во время активной записи в БД | «Фикси все по порядку» + аудит P1-8 | done | S5 |
| R11 | WebSocket системного звука закрывается при unmount, а не только при `capturing → false` | «Фикси все по порядку» + аудит P1-9 | done | S5 |
| R12 | API-ключ провайдера сохраняется целиком: запись при вводе не создаёт гонку | «Фикси все по порядку» + аудит P1-10 | done | S4 |
| R13 | Токены не уходят на хост после редиректа: доверие перепроверяется на конечном хосте | «Фикси все по порядку» + аудит P1-11 | done | S6 |
| R14 | Речь, содержащая «HTTP 404» и подобное, не считается ошибкой движка и не выбрасывается | «Фикси все по порядку» + аудит P1-12 | done | S6 |
| R15 | На Linux vocab-коррекции и плагин SQL работают с одной БД | «Фикси все по порядку» + аудит P1-13 | done | S3 |
| R16 | На macOS/Linux ключи не лежат открытым текстом; существующий plaintext-файл перешифровывается | «Фикси все по порядку» + аудит P1-14 | done | S3 |

### P2 — стоимость и мёртвый груз

| id | Требование | Цитата-источник | Статус | Слайс |
|---|---|---|---|---|
| R17 | Контекст приложения не создаёт новый объект значения на каждый рендер | «Фикси все по порядку» + аудит P2-15 | done | S7 |
| R18 | Стриминг ответа не пересчитывает и не перерисовывает весь список сообщений на каждый токен | «Фикси все по порядку» + аудит P2-16 | done | S7 |
| R19 | Barrel `@/components` не тянет recharts/cmdk в модули, которые их не используют | «Фикси все по порядку» + аудит P2-17 | done | S6 |
| R20 | Дублирование `useCompletion` / `useChatCompletion` устранено (один владелец логики) | «Фикси все по порядку» + аудит P2-18 | done | S5 |
| R21 | Режим portable не включается молча из-за записываемости каталога | «Фикси все по порядку» + аудит P2-19 | done | S3 |
| R22 | Захват звука не берёт два мьютекса на каждый сэмпл | «Фикси все по порядку» + аудит P2-20 | done | S2 |
| R23 | Осиротевший `FillerManager` удалён либо интегрирован (сейчас — второй механизм рядом) | «Фикси все по порядку» + аудит P2-21 | done | S6 |
| R24 | `resetRecentFillers` сбрасывает и английские индексы филлеров | «Фикси все по порядку» + аудит P2-22 | done | S6 |
| R25 | Таймер `withNoStream` перепроверяет владельца потока, а не заявляет поток себе вслепую | «Фикси все по порядку» + аудит P2-23 | done | S6 |
| R26 | Частично неудавшаяся миграция из localStorage не удаляет источник и не помечается завершённой | «Фикси все по порядку» + аудит P2-24 | done | S1 |
| R27 | Загрузка списка чатов не тянет всю историю сообщений одним `IN (…)` | «Фикси все по порядку» + аудит P2-25 | done | S1 |
| R28 | Выбор другого профиля промпта во время редактирования не сохраняет чужой черновик | «Фикси все по порядку» + аудит P2-26 | done | S7 |
| R29 | Визуализатор очищает canvas в тишине, а не морозит последние столбики | «Фикси все по порядку» + аудит P2-27 | done | S7 |
| R30 | Утечки/таймеры/сокеты после unmount устранены (4 места из аудита P2-28) | «Фикси все по порядку» + аудит P2-28 | done | S5 |

## Слайсы (вертикальные, по зонам — одному владельцу)

| Слайс | Требования | Зона |
|---|---|---|
| S1 | R01, R02, R06, R26, R27 | `src/lib/database/*` |
| S2 | R03, R04, R22 | `src-tauri/src/speaker/*` |
| S3 | R05, R07, R15, R16, R21 | `src-tauri/src/handy_server.rs`, `settings.rs`, `vocab.rs`, `activate.rs` |
| S4 | R08, R09, R12 | лицензия + секреты (UI/Rust) |
| S5 | R10, R11, R20, R30 | `src/hooks/*` (state/WS) |
| S6 | R13, R14, R19, R23, R24, R25 | security + barrel + филлеры |
| S7 | R17, R18, R28, R29 | UI-рендер |
