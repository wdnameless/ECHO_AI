# Tasks: portable-and-model-choice

## T1 — Пользовательские пути
- [x] T1.1 `settings.rs`: модель настроек (`data_root`, `engine_dir`,
      `models_dir`, `logs_dir`, `selected_model`, `start_minimized`)
- [x] T1.2 Разрешение корня: явный выбор → портативный (`ECHO_AI_HOME`,
      маркер `.portable`) → каталог пользователя
- [x] T1.3 Проверка права записи реальным пробником, а не по флагам
- [x] T1.4 Команды `get_paths` / `set_paths` / `pick_directory` /
      `enable_portable` / `disable_portable` / `get_start_minimized` /
      `set_start_minimized`
- [x] T1.5 База данных намеренно остаётся в каталоге пользователя (иначе
      обновление выглядело бы как потеря истории)

## T2 — Встраивание движка и портативная сборка
- [x] T2.1 `embedded.rs`: движок и 13 библиотек ggml через `include_bytes!`
- [x] T2.2 Распаковка в каталог, имя которого выведено из содержимого сборки
- [x] T2.3 Маркер завершённости пишется последним; прерванная распаковка
      повторяется
- [x] T2.4 Удаление распаковок прежних сборок
- [x] T2.5 `handy_server.rs`: движок ищется сначала во встроенном наборе,
      затем в пользовательском каталоге, затем в прежних местах установки
- [x] T2.6 `.gitignore`: бинарник движка отслеживается (нужен для сборки без
      сети), модель — нет
- [x] T2.7 CI: шаг портативного архива `Echo.AI_<версия>_portable_x64.zip`
- [ ] T2.8 Встраивание для Linux/macOS (в наборе пока только Windows-движок)

## T3 — Каталог и загрузка моделей
- [x] T3.1 `models.rs`: шесть квантизаций с размерами, SHA-256 и WER
- [x] T3.2 Загрузка по неизменяемой ревизии (не по ветке `main`)
- [x] T3.3 Запись во временный файл и переименование только после проверки
- [x] T3.4 Проверка SHA-256 потоком (2.38 ГБ не читаются в память целиком)
- [x] T3.5 Событие прогресса, не чаще ~10 Гц
- [x] T3.6 Выбор пользовательского `.gguf`; удаление с защитой от обхода пути
- [x] T3.7 Команды `model_catalog` / `list_models` / `selected_model` /
      `select_model` / `download_model` / `delete_model`
- [ ] T3.8 Докачка прерванной загрузки (сейчас начинается заново)

## T4 — Движок на выбранной модели
- [x] T4.1 `find_model_path`: выбранная модель → любой `.gguf` в каталоге
      моделей → прежние места установки (только для установленного варианта)
- [x] T4.2 `restart_server` с ожиданием освобождения порта
- [x] T4.3 Исправлено: остаток модели от прежней сборки больше не подхватывается
      из дерева сборки
- [x] T4.4 `stt_readiness`: сообщает, чего не хватает — модели или движка

## T5 — Интерфейс
- [x] T5.1 `app-paths.ts`: мост к командам, форматирование размеров
- [x] T5.2 Экран «Хранилище и модели»: переключатель портативного режима,
      выбор каталогов, тихий запуск
- [x] T5.3 Список моделей: размер, WER, признак «используется», прогресс
- [x] T5.4 Экран первого запуска: объяснение и кнопка загрузки рекомендованной
- [x] T5.5 Пользовательские `.gguf` в списке

## T6 — Сборка
- [x] T6.1 `tauri.conf.json`: модель убрана из `resources`
- [x] T6.2 CI: отказ от загрузки модели, проверка что модель не попала в бандл
- [x] T6.3 CI: проверка контрольных сумм движка (строки модели пропускаются)

## T7 — Верификация
- [x] T7.1 `cargo test` — 24 теста (каталог, пути, распаковка)
- [x] T7.2 `vitest` — 268 тестов
- [x] T7.3 `tsc --noEmit` — чисто
- [x] T7.4 Сборка релиза и запуск: движок распакован из exe, поднялся на
      Vulkan/RTX 3060
- [x] T7.5 Портативный режим проверен на чистой папке: файлы легли внутрь неё
- [x] T7.6 Загрузчик прогнан против Hugging Face: 495 МБ, хэш совпал,
      повторный вызов не качает, временный файл не остаётся
- [x] T7.7 Проверены все шесть URL каталога: коды и размеры совпадают
- [ ] T7.8 Ручной смоук установщика и портативного архива из релиза v1.1.0
