# 🚀 Echo AI — AI Copilot & Voice Mock Interview

<p align="center">
  <b>Умный AI-ассистент реального времени для встреч, собеседований и мок-интервью</b><br>
  <i>Lightning-fast, privacy-first AI desktop assistant powered by Tauri (Rust), React 19, and Vite.</i>
</p>

---

## 🌟 Основные возможности

### 🎙️ 1. Голосовое Mock Interview с RAG-контекстом
- **Адаптивные вопросы под вакансию**: ИИ выступает в роли интервьюера, задавая вопросы на основе загруженного **Резюме** и **Описания вакансии (Job Description)**.
- **Голосовой ответ**: Ответ кандидата записывается через микрофон с умным распознаванием речи.
- **Реалистичная разговорная генерация ответа («Generate answer»)**:
  - Формирует ответ от первого лица с естественными живыми вводными фразами (*«Ну, смотрите...»*, *«Слушайте, на практике...»* / *«Well, to be honest...»*, *«Yeah, in my previous role...»*).
  - Стриминг текста в реальном времени — слова появляются мгновенно.
  - Без роботизированных ИИ-клише и громоздких академических конструкций.
- **Оценка и разбор**: В конце интервью ИИ выставляет балл из 10, отмечает сильные стороны и точки роста.

### 🧠 2. RAG Context Storage (Резюме и Вакансии)
- Загрузка файлов `.pdf`, `.txt`, `.md` в разделе **Settings**.
- Автоматическое извлечение текста из PDF-документов.
- Мгновенное подключение/отключение контекста в системный промпт через тумблеры.

### ⚡ 3. Локальный STT (pluely-asr / Nemotron, Whisper)
- **Автозапуск локального STT**: при старте Echo AI поднимает нативный движок
  `pluely-asr.exe` и слушает его на `127.0.0.1:9877` (при занятом порту —
  9878…9882, выбранный порт пишется в `%APPDATA%/pluely/asr-port`).
- **Только локально**: облачные STT-провайдеры автоматически не вызываются.
  Если локальный сервер недоступен, запрос завершается явной ошибкой, а не
  уходит в облако. Python-мост (`handy_stt_server.py`) поднимается лишь как
  резервный путь, когда нативный движок не найден.

### 🔄 4. Встроенный модуль автообновлений (Tauri Auto-Updater)
- Проверка обновлений напрямую через **GitHub Releases** (`wdnameless/ECHO_AI`).
- Обновление одной кнопкой **«Download & Install Update»** прямо в интерфейсе без необходимости ручной переустановки.

---

## 🛠️ Архитектура и технологии

| Компонент | Стек |
|---|---|
| **Core / Desktop** | [Tauri v2](https://tauri.app/) (Rust) |
| **Frontend** | React 19, TypeScript, Tailwind CSS, Lucide Icons |
| **Локальная БД** | SQLite (`tauri-plugin-sql`) с автоматическими миграциями |
| **VAD / Аудио** | Silero VAD (`@ricky0123/vad-web`), Web Audio API |
| **STT Engine** | `pluely-asr` sidecar (Nemotron 3.5 ASR Streaming, Vulkan) + Whisper через Python-мост как резерв |
| **Автообновления** | `tauri-plugin-updater` с криптографической подписью Minisign |

---

## 🚀 Установка и запуск из исходников

### Требования
1. **Node.js**: `v20+`
2. **Rust & Cargo**: `stable` (`rustup default stable`)
3. **Python**: `3.10+` — необязателен. Нужен только как резервный STT-мост,
   если нативный `pluely-asr.exe` не найден.
4. **Модель ASR**: скачивается на этапе сборки (см. `.github/workflows/release.yml`),
   в рантайме уже лежит внутри бандла.

### Установка зависимостей
```bash
npm install
```

### Запуск в режиме разработки
```bash
npm run tauri dev
```

### Сборка релизного инсталлятора
```bash
npm run tauri build
```
Готовые установщики появятся в:
- `src-tauri/target/release/bundle/nsis/Echo AI_x.x.x_x64-setup.exe`
- `src-tauri/target/release/bundle/msi/Echo AI_x.x.x_x64_en-US.msi`

Имя самого бинарника остаётся `pluely.exe` — так установленные копии сохраняют
данные и канал обновлений.

---

## ⚙️ Настройка провайдеров и ключей

1. **AI Провайдеры**:
   - Откройте раздел **Dev Space → AI Providers** и укажите свой API-ключ для OpenAI, Anthropic, DeepSeek, Groq или локальной Ollama.
2. **Резюме и Вакансия**:
   - Перейдите в **Settings** → блоки **My Resume** и **Job Description**.
   - Нажмите **Upload document** и выберите ваш PDF или текстовый файл.
3. **STT (Распознавание речи)**:
   - **Локальный движок**: используется по умолчанию, отдельная настройка не нужна.
   - **Свой облачный STT**: добавьте провайдера в разделе **Dev Space → STT Providers**.
     Учтите, что конвейер диктовки намеренно работает только локально.

---

## 💎 Free и Pro

Приложение распространяется по модели freemium. Бесплатный тариф — это
локальное ядро, и он не урезан по времени:

| Бесплатно навсегда | Pro |
|---|---|
| Диктовка и режим встречи | Хостируемый Echo API (без своего ключа) |
| Локальное распознавание речи (Nemotron/Whisper) | Скриншот и режим выделения области |
| Свой AI-ключ (BYO-key) | Темы, шрифты, прозрачность |
| История чатов | Кастомные шорткаты |
| Свои системные промпты | Генерация промптов, аналитика дашборда |
| | Настройки длины и языка ответа |

Оплата пока не подключена: экран апгрейда прямо сообщает, что покупка
недоступна, вместо имитации оплаты. В dev-сборке открыты все возможности,
в релизной — Pro-функции показывают пояснение о тарифе.

---

## 📦 Репозиторий и автообновления

- **GitHub Repository**: [https://github.com/wdnameless/ECHO_AI](https://github.com/wdnameless/ECHO_AI)
- **CI/CD**: workflow запускается по тегу `v*` (или вручную через
  `workflow_dispatch`). Он собирает приложение под Windows, подписывает
  артефакты ключом `TAURI_SIGNING_PRIVATE_KEY` и публикует GitHub Release
  с манифестом `latest.json`. Push в `master` сам по себе релиз не создаёт.
- **Обновление в приложении**: При появлении нового релиза в окне Echo AI отображается кнопка обновления, скачивающая и применяющая апдейт на лету.

---

## 📄 Лицензия

Проект распространяется под лицензией **GNU General Public License v3.0**
(см. [LICENSE](LICENSE)).

### Происхождение и изменения (GPL-3.0 §5)

Echo AI — модифицированная версия [Pluely](https://github.com/iamsrikanthnani/pluely)
© 2025 Srikanth Nani, распространявшегося под той же лицензией GPL-3.0.

Изменения относительно оригинала:

- продукт переименован в **Echo AI** (отображаемое имя, версия 1.0.0);
- добавлен контур безопасности: строгая CSP, собственный санитайзер разметки
  вместо `rehype-raw`, контроль исходящих запросов на незнакомые хосты;
- ключи провайдеров и веб-поиска перенесены в защищённое хранилище ОС;
- введена модель freemium с реестром возможностей Free/Pro;
- локальный движок распознавания речи стал единственным путём STT.

Исходный код доступен в этом репозитории — как того требует лицензия.
